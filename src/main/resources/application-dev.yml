#Database configuration for MySql Dev
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/beer_service_dev?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC
    username: beer_service_user
    password: password
    platform: mysql
  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: false